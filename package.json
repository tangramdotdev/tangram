{
	"dependencies": {
		"@biomejs/biome": "^2.3.8",
		"@types/bun": "^1.3.3",
		"esbuild": "^0.27.0",
		"typescript": "^5.9.3"
	},
	"private": true,
	"scripts": {
		"canary": "bun scripts/canary.ts",
		"check": "cargo clippy --all-features --all-targets --workspace && bun run --elide-lines=0 --filter \"*\" check",
		"clean": "rm -rf node_modules target",
		"cloud:up": "docker compose -f scripts/cloud/compose.yaml up",
		"cloud:init": "nu scripts/cloud/init.nu",
		"cloud:deinit": "nu scripts/cloud/deinit.nu",
		"cloud:rm": "docker compose -f scripts/cloud/compose.yaml rm",
		"cloud:server": "nu scripts/cloud/server.nu",
		"cloud:runner": "nu scripts/cloud/runner.nu",
		"format": "cargo fmt --all && bun run --elide-lines=0 --filter \"*\" format",
		"test": "nu packages/cli/test.nu $@ && cargo nextest run --no-fail-fast --workspace && bun run --elide-lines=0 --filter \"*\" test",
		"tg": "cargo run -- -m client",
		"tgo": "cargo build --target aarch64-unknown-linux-gnu && orb sh -c './target/aarch64-unknown-linux-gnu/debug/tangram $@' -- -m client",
		"tgor": "cargo build --release --target aarch64-unknown-linux-gnu && orb sh -c './target/aarch64-unknown-linux-gnu/release/tangram $@' -- -m client",
		"tgorx": "cargo build --release --target aarch64-unknown-linux-gnu && orb sh -c './target/aarch64-unknown-linux-gnu/release/tangram $@' -- -m client run -b",
		"tgos": "cargo build --target aarch64-unknown-linux-gnu && orb sh -c './target/aarch64-unknown-linux-gnu/debug/tangram $@' -- -m server",
		"tgr": "cargo run --release -- -m client",
		"tgrs": "cargo run --release -- -m server",
		"tgrsx": "cargo run --release -- -m server run -b",
		"tgrx": "cargo run -- -m client run -b",
		"tgs": "cargo run -- -m server",
		"tgsx": "cargo run -- -m server run -b",
		"tgx": "cargo run -- -m client run -b"
	},
	"workspaces": [
		"packages/clients/js",
		"packages/js",
		"packages/typescript",
		"packages/vscode"
	]
}
